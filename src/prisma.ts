


// import { Currency, PaymentType, PrismaClient, Status } from "@prisma/client";
import { Prisma } from './Utils/Prisma';
// import { PrismaClient } from '@prisma/client';





async function main() {

    // const sel = await Prisma.client.user.findMany();

    // console.log(sel);

    // await Prisma.client.$disconnect()

    // const insert = Prisma.

    // const insert = await prisma.user.create({
    //     data: {
    //         name: 'user1'
    //     }
    // })

    // if (insert.id) {
    //     console.log("user careet")
    //     console.log(`user ip: ${insert.id}`)
    // }

    // const select = await Prisma.client.user.findMany()

    // console.log(select)

    // const countUsers = await prisma.user.count({});

    // console.log(countUsers);
  }

  main();
  
//   main()
//     .then(async () => {
//       await Prisma.client.$disconnect();
//     })
//     .catch(async (e) => {
//       console.error(e);
//       await Prisma.client.$disconnect();
//       process.exit(1);
//     });